[package]
name = "web-forge"
version = "0.1.0"
edition = "2024"
description = "Rust implementation of the WebForge parser and runtime"
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
